class AddCpfTelefoneEmailToPacientes < ActiveRecord::Migration[8.0]
  def change
    # Columns already exist, so this migration is no longer needed
    # You can safely remove this migration file after confirming the schema
  end
end
